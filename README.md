# Joan500

Vintage second-hand store website for Joan500.

## Run locally

```bash
node server.js
```

Then open `http://127.0.0.1:5173`.

Product images live in `img/` and the logo image lives in `logo/`.
